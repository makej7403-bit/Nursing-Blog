# Nursing Hub

Next.js (pages router) + Firebase + OpenAI scaffold for a nursing courses & notes site.
Follow .env.example to set environment variables. Do not commit secrets.

Run:
npm install
npm run dev
